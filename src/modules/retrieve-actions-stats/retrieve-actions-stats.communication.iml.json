[
  {
    "url": "/stats/actions",
    "method": "GET",
    "qs": {
      "start": "{{parameters.startDate}}",
      "end": "{{parameters.endDate}}"
    },
    "response": {
      "output": {
        "data": "{{body.data}}"
      }
    }
  }
]
