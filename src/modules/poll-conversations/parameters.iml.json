[
  {
    "name": "conversations",
    "type": "array",
    "label": "Conversations",
    "required": true,
    "help": "List of conversations to poll for new messages",
    "spec": [
      {
        "name": "personUrl",
        "type": "text",
        "label": "Person URL",
        "required": true,
        "help": "LinkedIn URL of the person whose conversation you want to poll",
        "placeholder": "https://www.linkedin.com/in/john-doe"
      },
      {
        "name": "since",
        "type": "date",
        "label": "Since Date",
        "required": false,
        "help": "Only retrieve messages since this date (ISO 8601 format)",
        "placeholder": "2024-01-15T10:30:00Z"
      },
      {
        "name": "type",
        "type": "select",
        "label": "Interface Type",
        "required": true,
        "help": "Choose which LinkedIn interface to use",
        "default": "st",
        "options": [
          { "value": "st", "label": "Standard Interface" },
          { "value": "nv", "label": "Sales Navigator" }
        ]
      }
    ]
  }
]
