[
  {
    "url": "/conversations/poll",
    "method": "POST",
    "body": {
      "conversations": "{{parameters.conversations}}"
    },
    "response": {
      "output": {
        "data": "{{body}}"
      }
    }
  }
]
