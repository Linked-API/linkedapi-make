[
  {
    "url": "/conversations/poll",
    "method": "POST",
    "body": "{{parameters.conversations}}",
    "response": {
      "output": {
        "data": "{{body.data}}",
        "errors": "{{body.errors}}"
      }
    }
  }
]
