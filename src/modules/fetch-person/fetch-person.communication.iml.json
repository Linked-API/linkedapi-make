[
  {
    "url": "/workflows",
    "method": "POST",
    "body": {
      "operationName": "fetchPerson",
      "data": {
        "personUrl": "{{parameters.personUrl}}",
        "retrieveExperience": "{{parameters.retrieveExperience}}",
        "retrieveEducation": "{{parameters.retrieveEducation}}",
        "retrieveSkills": "{{parameters.retrieveSkills}}",
        "retrieveLanguages": "{{parameters.retrieveLanguages}}",
        "retrievePosts": "{{parameters.retrievePosts}}",
        "retrieveComments": "{{parameters.retrieveComments}}",
        "retrieveReactions": "{{parameters.retrieveReactions}}",
        "postsRetrievalConfig": {
          "limit": "{{parameters.postsLimit}}",
          "since": "{{parameters.postsSince}}"
        },
        "commentsRetrievalConfig": {
          "limit": "{{parameters.commentsLimit}}",
          "since": "{{parameters.commentsSince}}"
        },
        "reactionsRetrievalConfig": {
          "limit": "{{parameters.reactionsLimit}}",
          "since": "{{parameters.reactionsSince}}"
        }
      }
    },
    "response": {
      "temp": {
        "workflowId": "{{body.workflowId}}",
        "operationName": "fetchPerson",
        "status": "{{body.status || body.workflowStatus}}",
        "message": "{{body.message}}",
        "workflowStatus": "{{body.workflowStatus || body.status}}",
        "criticalError": "{{body.criticalError}}"
      }
    }
  },
  {
    "url": "/workflows/{{temp.workflowId}}?operationName={{temp.operationName}}",
    "method": "GET",
    "repeat": {
      "condition": "{{(body.workflowStatus || body.status) === 'pending' || (body.workflowStatus || body.status) === 'running'}}",
      "delay": 5000,
      "limit": 1800
    },
    "response": {
      "temp": {
        "status": "{{body.status || body.workflowStatus}}",
        "data": "{{body.data}}",
        "errors": "{{body.errors}}",
        "message": "{{body.message}}",
        "workflowStatus": "{{body.workflowStatus || body.status}}",
        "criticalError": "{{body.criticalError}}"
      }
    }
  },
  {
    "response": {
      "valid": "{{(temp.workflowStatus || temp.status) === 'completed'}}",
      "error": {
        "message": "{{temp.criticalError.message || temp.message || 'Workflow timed out or did not complete.'}}"
      },
      "output": {
        "data": "{{temp.data}}",
        "errors": "{{temp.errors}}"
      }
    }
  }
]
